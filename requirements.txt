# Haru Checker Bot — Python dependencies
# Install: pip install -r requirements.txt

python-telegram-bot>=21.0.1,<22.0
requests>=2.31.0
python-dotenv>=1.0.0

# Catatan: httpx dipakai bot.py lewat `telegram.request.HTTPXRequest`.
# httpx sudah otomatis terpasang sebagai dependency python-telegram-bot.
